# 无感置换计算器

用浏览器打开本仓库 GitHub Pages 地址即可多端使用（手机 / 平板 / PC）。

## 本地预览

直接双击打开 `index.html`，或在本目录执行：

```bash
npx --yes serve .
```

## 更新发布

1. 在上级目录运行 `node merge-single.js`
2. 将生成的 `置换计算器_分享版.html` 复制覆盖为 `gh-pages/index.html`
3. 提交并推送到已开启 GitHub Pages 的分支
